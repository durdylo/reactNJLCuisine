import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { UserContext } from './App'
export default function Home() {
    const 
    return (
        <View>
            <Text>Bienvenu {user.username || 'mr.user'}</Text>
        </View>
    )
}

const styles = StyleSheet.create({})